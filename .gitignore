# heavy / generated
node_modules/
.next/
__pycache__/
*.pyc
.venv/
.env
.DS_Store
test-results/
# prototype has its own git repo
# media & raw sources (not code)
mocs/
new docs/
docs/*.pdf
docs/*.docx
docs/references/
*.m4a
*.xlsx
Juicy_документы/*.docx
backend/.venv/
backend/juicy.db
app/.git-prototype-backup/
app/node_modules/
app/.next/
app/test-results/
app/tsconfig.tsbuildinfo

# local sqlite DBs
backend/*.db

# uploaded media (admin drag-and-drop)
backend/uploads/

# legacy reference web project (removed from tree)
docs/grabzi-web/

# prod deploy: built standalone bundles (собираются локально, доставляются на сервер) + секрет
deploy/web/
deploy/front/
deploy/.env
