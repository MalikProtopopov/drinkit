# TEST_ISSUE: e2e-тесты прототипа vs V2

Существующие 44 Playwright-теста написаны под frontend-only прототип (mock-данные,
размеры S/M/L, экраны outlets, авто-таймеры статусов). После подключения к API (V2)
часть сценариев устарела:
- каталог/деталка теперь требуют запущенный backend (uvicorn :8000);
- размеры S/M/L и слоты выдачи удалены из V2-флоу (нет в требованиях);
- статусы приходят по WebSocket, а не по таймерам.

TODO: переписать golden-path под V2 (запуск через `docker compose up` + `npm run dev`).
Backend-логика покрыта 34 pytest-тестами в /backend/tests.
