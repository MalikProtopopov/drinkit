# GRABZI — план реализации (v9, исполняемый)

> **Версия v9:** + зафиксирован **стек** (PostgreSQL/Poetry/FastAPI/Next.js/Docker, Р0.1); перепроверен **парсинг живого сайта** (§1.3: карточки — статичные **PNG, не видео** Р3.0; палитра — реальная мульти-схема §3.2); добавлены **две детальные спеки** (§13) — [фронт-архитектура+UI-кит+состояния+экран готовки](GRABZI_FRONTEND_SPEC.md) и [бэкенд-архитектура+тесты](GRABZI_BACKEND_AND_TESTING.md); сквозные фиксы ревизии — **WS-аутентификация** (блокер), **канонический реестр кодов ошибок**, **лимит в напитках**, refund-корректность, **VAT-inclusive**, Alembic 0001/0002/0003.
> **v8:** детальный дизайн доработок админки (§5.12–§5.17): MinIO+drag-and-drop, пауза super_admin, стоп-лист, панель менеджера, настройки; фундамент локаций + Alembic.

> **Суть (главное).** Делаем проект **GRABZI на базе готового кода Juicy**: берём **бэкенд и админку Juicy** как основу и **дорабатываем под требования GRABZI** (локации, лимиты, часы, привязка сотрудников, аналитика по точкам). Поверх — **отдельный фронтенд GRABZI** в дизайне GRABZI (desktop + mobile), подключённый к этому бэкенду. Что в GRABZI не нужно (добавки, купоны, онбординг, ввод имени, корзина) — просто не используем; код Juicy не ломаем.
>
> **Источники.** Парсинг `grabzi.ae` (Shopify) и `order.grabzi.ae` (React/Supabase/Stripe SPA), аудит проекта Juicy (`app/` — Next.js app-router, `backend/` — FastAPI + SQLAlchemy). Данные парсинга — в `grabzi_parser/`.
>
> Документ самодостаточен: подход, модель данных, контракты API, карта экранов и компонентов, фазы с критериями приёмки.

---

## 0. Принятые решения (зафиксировано — не пересматриваем)

| # | Решение |
|---|---|
| Р1 | **UI-кит — GRABZI.** Существующие (спарсенные) экраны воспроизводим как есть. Недостающие экраны берём из Juicy **по логике/структуре**, но собираем на **компонентах GRABZI**. |
| Р2 | **Две версии вёрстки — desktop и mobile** (адаптив/респонсив, см. §3). |
| Р0 | **Берём готовый бэкенд+админку Juicy как основу и дорабатываем под GRABZI.** Не пишем заново — используем существующий код Juicy (FastAPI+SQLAlchemy + админка) и расширяем его требованиями GRABZI (локации, лимиты, часы, привязка сотрудников, аналитика). Фронт GRABZI — отдельный, в дизайне GRABZI. То, что в GRABZI не нужно (добавки, купоны), просто не используем. |
| Р0.1 | **Стек зафиксирован:** **PostgreSQL** (БД), **Poetry** (зависимости Python), **FastAPI** (бэкенд), **Next.js** (фронт), **Docker** (compose: postgres + redis + minio + backend). Текущий Juicy на SQLite-дефолте/`requirements.txt`/`create_all` → мигрируем на Postgres + Poetry + **Alembic** (§5.12, §8А). Чистая архитектура и тесты — §8А, §13. |
| Р3 | **Без добавок.** Публичный каталог = категории напитков + напитки (тот же формат карточек). Добавки/KБЖУ-добавки **не выводим**. Логика **каталога берётся из Juicy**. Бэкенд-сущности добавок дремлющие (§4). |
| Р3.0 | **Карточки — статичные изображения (PNG), НЕ видео.** Проверено на живом grabzi.ae: товары — PNG-стикеры (квадрат/прямоугольник), видео на сайте нет. Прежнее допущение «видео-петли» из Juicy-моков — **отменено** (поле `video_url` поддерживаем опционально, но GRABZI его не использует). |
| Р3.1 | **Создание заказа — одна страница** (не корзина/деталка): список напитков выбранной категории сразу на экране, у каждого `+/–` счётчик; внизу — контакты (телефон, авто), итог и кнопка оплаты. Перед ней — выбор локации (§4). |
| Р3.2 | **Деталка напитка — отдельный опциональный модуль.** Не обязателен. Если включён — экран деталки напитка (как в основном проекте) с кнопкой «Create order», которая ведёт в создание заказа с предзаполнением. |
| Р4 | **Локации — first-class.** Админка создаёт локации (описание, адрес, часы по дням, лимит); публично — выбор локации с остатком перед созданием заказа; при оплате — атомарная проверка лимита. |
| Р5 | **Заказ виден менеджеру и списывает лимит ТОЛЬКО после оплаты.** На «оплатить» создаётся заказ с `payment_status="pending"` + платёж; **до оплаты он скрыт от менеджеров и из статистики** (Juicy уже фильтрует по `payment_status="paid"`), **лимит не трогает**. После успешной оплаты → `payment_status="paid"` (доступен к приготовлению) + списывает лимит. Это исключает «мусорные» неоплаченные заказы. Лимит считается **в напитках** (`sum quantity`); refund **возвращает** напитки в лимит того дня (§5.4). **Заказ фиксирует `location_id`** (аналитика/история). Реализуется на существующем `payment_status` Juicy — без нового статуса (§5.1). |
| Р5.1 | **Дневной лимит — необязательный.** Задан → проверяется; **не задан (`null`) → без лимита** (объём всё равно учитывается для аналитики). |
| Р6 | **Рабочие часы локации блокируют заказ.** Вне окна работы точки заказ недоступен (per-location `is_open`). |
| Р7 | **Каталог одинаков для всех локаций** (матрицу «напиток × локация» не делаем). |
| Р8 | **Купоны — НЕ нужны** в GRABZI. Оценка 👍/👎 **остаётся**, но **без выпуска купона** (в Juicy дизлайк выпускал купон — для GRABZI отключаем). Купонные сущности дремлющие. |
| Р9 | **Онбординг и «ввод имени после регистрации» — НЕ нужны** (ни Juicy, ни GRABZI). Убираем из флоу. |
| Р10 | **«Мои заказы»** с двумя состояниями: авторизован → список заказов; не авторизован → предложение создать заказ **или войти по номеру телефона** (тот же номер, что в прошлых заказах) для подгрузки истории (§4.4). |
| Р11 | **Профиль / личный кабинет** — как в основном Juicy: имя, авто, настройки. **Без купонов.** |
| Р12 | **Публичный сайт GRABZI — только английский.** Переключатель скрыт. Админка — RU/AR временно. Арабская версия — отдельный отложенный модуль (§3.3), сейчас только дешёвый no-regret задел. |
| Р13 | **Сотрудник привязывается к локации.** Супер-админ прикрепляет сотрудника к точке. **Менеджер видит/обрабатывает заказы только своей локации**; супер-админ — все. Локация менеджера видна в аналитике (§5.9). |

**Открытые (не блокируют старт, есть дефолты):**
- Имя инфо-страницы: дефолт `/info`. Контент из админки: дефолт — да (`info_blocks`).
- Деталка напитка (Р3.2): дефолт — модуль **выключен** на старте, включается флагом.
- Apple Pay: включаем, если настроен Stripe-аккаунт (фиче-флаг).

---

## A. Подход: дорабатываем готовый бэкенд Juicy под GRABZI (Р0)

**Логика простая, как у разработчика:**
- Берём **готовый бэкенд и админку Juicy** (FastAPI+SQLAlchemy + админ-панель) — это **основа** проекта GRABZI.
- **Дорабатываем их под требования GRABZI:** добавляем локации, дневные лимиты, рабочие часы по дням, привязку сотрудников к точке, доступ менеджера к своей точке, аналитику по локациям (всё ниже по документу).
- Делаем **отдельный фронтенд GRABZI** в дизайне GRABZI и подключаем к этому бэкенду.
- То, что у GRABZI **не нужно** (добавки, купоны, онбординг, ввод имени, корзина) — **просто не используем** на фронте GRABZI; в коде Juicy это остаётся нетронутым (ничего не удаляем, чтобы не ломать).

**Что меняем, что не трогаем:**
- *Меняем/добавляем (бэкенд+админка):* новый раздел «Локации», поля и счётчики лимитов, часы по дням, `orders.location_id` + списание лимита в `mark_paid` (на существующем `payment_status`), гостевой заказ по телефону (§4.7), привязка сотрудника к локации, фильтр заказов по локации для менеджера, аналитика по точкам.
- *Не трогаем:* модели и эндпоинты каталога/заказов/оплат/пользователей — переиспользуем как есть.
- *Фронт GRABZI:* новый, берёт логику экранов из Juicy, но рисует на компонентах GRABZI и обращается к доработанному бэкенду.

> Без env-переключателей брендов и хитрых схем развёртывания: это **проект GRABZI на базе кода Juicy**. Juicy-проект живёт своей жизнью отдельно; здесь мы делаем GRABZI, переиспользуя его готовый бэкенд.

---

## 1. Что спарсили (фактура)

### 1.1. `grabzi.ae` (Shopify, публичный сайт)
| Страница | URL | Содержимое |
|---|---|---|
| Главная | `/` | Hero, бренд, 6 секций, навигация |
| Меню | `/pages/menu` | Напитки с ценами (Classic 28, Choco berry 34, Melon spark 37, …) |
| Story | `/pages/story/...` | «Our Story» |
| Working hours | `/pages/working-hours/...` | Пн–Сб 5:30–22:00, Вс 10:00–18:00 |
| Contact us | `/pages/contact-us` | `grabzi150@gmail.com`, `+971556676679` |
| Товар | `/products/{handle}` | 2 товара (Cherry Bomb 36, Classic 28 AED), карточки с медиа |

### 1.2. `order.grabzi.ae` (React SPA — «страница создания заказа»)
Экраны: `/`, `/login`, `/payment`, `/success`, `/customer-history`, `/admin`, `/manager`, `/manager-login`, `/staff`.
RPC: `get_daily_cups_sold` (→ `total_cups_sold`, `remaining_cups`, `is_sold_out`), `can_place_order`, `is_shop_open`, `is_free_treat_day`, `has_role`, `validate_manager_session`, `end_manager_session`.

**Вывод.** Дневной лимит у GRABZI **уже есть, но глобальный (одна точка)**; «📍 LOCATION» — статичный адрес, не выбор. Мультилокационность + per-location рабочие часы — наш новый функционал.

### 1.3. Верификация парсинга (повторная проверка живого сайта)
Перепроверено curl-ом по живому `grabzi.ae` — что забрал парсер `grabzi_parser/` корректно, а что нужно уточнить:
| Что | Статус | Деталь |
|---|---|---|
| Каталог (товары/цены) | ✅ верно | `products.json` — 2 товара (Cherry Bomb 36, Classic 28 AED); меню-страница — расширенный список (Choco berry 34, Melon spark 37 …). |
| Страницы/навигация | ✅ верно | главная + `menu/story/working-hours/contact-us`; часы Пн–Сб 5:30–22:00 / Вс 10:00–18:00; контакты `grabzi150@gmail.com`, `+971556676679`. |
| Шрифты | ✅ + уточнение | `Grenadine MVB Black` + `Extra Bold Italic` (woff2 на Shopify CDN); **лицензия «fonnts.com» под вопросом** → self-host/замена (§3.2). |
| **Палитра/стили** | ⚠️ было неполно → **дополнено** | парсер дал общий HTML; реальная палитра — **мульти-схема** (терракота/крем/бирюза/олива + stock-цвета), зафиксирована в §3.2 и спеке. |
| **Медиа карточек** | ⚠️ исправлено допущение | товары — **статичные PNG-стикеры** (933×577, 1182×1182), **видео на сайте НЕТ**. Прежнее «видео-петли» отменено (Р3.0). |
| Видео-петли | ❌ их нет | допущение пришло из Juicy-моков, не из GRABZI. |

> Итог: парсинг каталога/страниц/контактов корректен; скорректированы два момента — **палитра расширена** до реальной мульти-схемы и **медиа = PNG, не видео**. Дизайн-детали вынесены в [docs/GRABZI_FRONTEND_SPEC.md](GRABZI_FRONTEND_SPEC.md).

---

## 2. Стратегия сборки: что воспроизводим, что добавляем

Ключевой принцип (Р1): **два класса экранов.**

- **Класс A — «есть в GRABZI» → воспроизводим как есть** на нашем стеке: верстаем по спарсенному дизайну GRABZI (бренд, шрифты, цвета, контент), подключаем к нашему API. Не редизайним.
- **Класс B — «нет в GRABZI, есть в Juicy» → добавляем недостающее**: берём из Juicy **логику и структуру экрана**, но **перебираем на компонентах GRABZI** (UI-кит GRABZI), в desktop+mobile.

Колонка «Действие» в §3 размечена этими классами.

---

## 3. Платформа и дизайн-система

### 3.1. Desktop + mobile (Р2)
- **Подход:** единый адаптивный фронт (Next.js), брейкпоинты `mobile < 768 ≤ tablet < 1024 ≤ desktop`. Не делаем отдельных кодовых баз.
- **Навигация:**
  - mobile — нижний таб-бар (GRABZI-аналог `BottomNav`): Home / Menu / Orders / Profile;
  - desktop — верхний header-nav (новый компонент `GrabziHeader`) с теми же разделами + CTA «Create order» и выбранная локация с остатком (корзины нет — заказ одностраничный, Р3.1).
- **Сетки:** каталог — 2 колонки на mobile, 3–4 на desktop; деталка напитка — одна колонка на mobile, две (медиа слева / инфо справа) на desktop.
- **Тач/ховер:** на desktop — ховер-состояния карточек (лёгкий зум/тень статичного PNG); на mobile — тап. Видео нет (Р3.0).

### 3.2. UI-кит GRABZI (Р1) и карта компонентов
> Детальный UI-кит, дизайн-токены, состояния компонентов и фронт-архитектура — в отдельном файле **[docs/GRABZI_FRONTEND_SPEC.md](GRABZI_FRONTEND_SPEC.md)** (на который ссылается план). Ниже — выжимка.

Заводим брендовый кит `components/ui/*` + тему через CSS-переменные/Tailwind-theme. **Палитра — реальная, проверена на живом grabzi.ae** (мульти-схема, не один акцент):
| Токен | Значение | Назначение |
|---|---|---|
| `--brand` | `#C44429` (терракота) | основной бренд, CTA-фон |
| `--brand-strong` | `#A42325` / `#780406` | тёмно-красные заголовки/нажатые CTA |
| `--cream` | `#E8D5C7` · `--cream-yellow` `#FFF8D4` | фоны секций/карточек |
| `--teal` | `#29C4B4` · `--teal-soft` `#98C3C9` | акцент-схема (доп. секции) |
| `--olive` | `#B5BD62` | акцент-схема |
| `--ink`/`--paper` | `#000`/`#FFF` · `--muted` `#F1F1F1` | текст/фон |
| `--instock`/`--lowstock`/`--outofstock` | `#3ED660`/`#EE9441`/`#C8C8C8` | бейджи остатка/sold-out (готовые токены темы!) |
- Шрифты: `Grenadine MVB Black` (900) + `Grenadine MVB Extra Bold Italic` — **self-host woff2** (лицензия «fonnts.com» под вопросом → проверить/заменить близким display-шрифтом как fallback).
- Тон: эмодзи-нейминг (`🍒💣 🧊 🍫🍇 🍉⚡️`), drive-through, EN-only. **Карточки — статичные PNG (Р3.0), не видео.**

**Соответствие компонентов (Juicy → GRABZI-кит):**
| Назначение | Juicy-компонент | GRABZI-замена |
|---|---|---|
| Шапка экрана | `TopBar` | `GrabziTopBar` (mobile) / `GrabziHeader` (desktop) |
| Нижняя навигация | `BottomNav` | `GrabziBottomNav` (mobile) + header (desktop) |
| Карточка напитка | `ApiProductCard` | `GrabziDrinkCard` (статичный PNG, цена, эмодзи-имя, `+/–`) |
| Степпер кол-ва | `StepperButton` | `GrabziStepper` |
| Модалка/шторка | `BottomSheet` / `Modal` | `GrabziSheet` / `GrabziModal` |
| Тосты | `useToast` | переиспользуем, ре-стайл |
| Бейдж остатка | — (новый) | `RemainingBadge` («N left today») |
| Бейдж статуса точки | — (новый) | `LocationStatusBadge` («Closed · opens 5:30») |

**Админка** компонентов GRABZI **не получает** — остаётся на `AdminShell`/`Modal`/`admin-*` стилях Juicy (Р8).

### 3.3. Арабская локализация — отдельный отложенный модуль (Р9)
**Статус: под вопросом у заказчика, в текущий скоуп НЕ входит.** База публичного сайта — **чисто английская**. Чтобы будущий арабский не потребовал переписывания, делаем **только дешёвый no-regret задел** (он почти не удорожает EN-разработку), а всё «дорогое» по AR выносим в отдельный модуль, который запускается **только по подтверждению заказчика**.

**A. Обязательный сейчас (дешёвый задел, без удорожания):**
- **Никакого хардкода UI-строк** в JSX — все строки через словарь `messages/en.json` (структура ключей готова, заполнен только `en`).
- **Логические CSS-свойства** вместо физических (`margin-inline-start/end`, `padding-inline`, `text-align: start/end`, `inset-inline`) — это и для LTR корректно, и даёт RTL «бесплатно».
- **Контент-поля в БД — JSON i18n** `{ "en": "..." }` (схема Juicy уже такая) — добавление `ar` позже не меняет схему.
- API контента принимает `?locale=` с фолбэком на `en`.

**B. Отдельный модуль «Арабская версия» (НЕ сейчас, только если заказчик подтвердит):**
- Локаль-роутинг `/(en|ar)/`, флаг `public_locales=["en","ar"]`, `<html lang dir>`.
- RTL-вёрстка и приёмка всех публичных экранов в `dir="rtl"`, зеркалирование иконок/прогресса.
- Заполнение `messages/ar.json` и `ar`-контента (переводы напитков/инфо).
- Арабский шрифтовой стек (напр. `IBM Plex Sans Arabic`/`Noto Kufi Arabic`), отдельные типографские токены, условное подключение по `lang="ar"`.
- Переключатель языка в публичном UI, форматирование чисел/дат через `Intl` для `ar`.

> Граница ответственности: пункт **A делаем в Фазе 1** (почти бесплатно), пункт **B — отдельная оценка и фаза, вне базового скоупа**. Так EN→EN+AR останется «наполнение данными + флаг + RTL-приёмка», но мы не платим за это сейчас.

**Маршрутизация и определение языка**
- Закладываем локаль-сегмент в URL: `/(en|ar)/...` (Next.js i18n routing / middleware). Сейчас активна только `en`; `ar` зарезервирована и скрыта за флагом `public_locales=["en"]` (на будущее `["en","ar"]`).
- Источник локали: сегмент URL → `<html lang>` и `dir`. EN → `dir="ltr"`, AR → `dir="rtl"`.

**RTL-вёрстка (главное для AR)**
- **Никаких физических `left/right/margin-left/padding-right`** в стилях публичной части — только **логические свойства** (`margin-inline-start/end`, `inset-inline`, `text-align: start/end`, `padding-inline`). Это даёт зеркалирование «бесплатно» при `dir="rtl"`.
- Иконки и направленные элементы (стрелки «назад», прогресс заказа, карусели) — зеркалируются по `:dir(rtl)` / `[dir="rtl"]`-селекторам.
- Flex/grid — направление потока через `flex-direction`/логические выравнивания, чтобы при RTL не ломалось.
- Тестовый чек: каждый публичный экран проверяется в `dir="rtl"` (storybook-knob / тестовый флаг) уже сейчас, чтобы не накопить LTR-долг.

**Тексты UI**
- Все строки интерфейса — **через словарь переводов** (`messages/en.json`, зарезервирован `messages/ar.json`), **без хардкода** строк в JSX. Сейчас заполнен только `en`. Используем `next-intl` / лёгкий собственный словарь.
- Числа/даты/валюта — через `Intl` с локалью (AED форматирование корректно и для `ar`).

**Контент (напитки, инфо-страница)**
- Поля контента в БД делаем **локализуемыми**: `drinks.name`, `drinks.description`, `drink_categories.name`, `info_blocks.title/body` хранятся как JSON `{ "en": "...", "ar": "..." }` (инфраструктура i18n Juicy уже такая). Сейчас заполняем только `en`; `ar` добавляется позже без миграции схемы.
- API отдаёт контент по запрошенной локали (`?locale=en|ar`), с фолбэком на `en`.

**Шрифты**
- Латинский бренд-шрифт `Grenadine MVB` не покрывает арабицу. Заранее закладываем **арабский шрифтовой стек** (напр. `IBM Plex Sans Arabic` / `Noto Kufi Arabic`) и подключаем его условно при `lang="ar"`. Размеры/межстрочные — отдельные токены для AR (арабская типографика «выше»).

**Что делаем сейчас vs потом**
- *Сейчас:* словарь `en`, логические CSS-свойства, локаль-роутинг с одной активной локалью, JSON-контент с ключом `en`, RTL-прогон в тестах.
- *Потом (включение AR):* заполнить `messages/ar.json` и `ar`-контент, добавить `ar` в `public_locales`, подключить арабский шрифт, включить переключатель языка, прогнать RTL-приёмку.

> Итог: переход EN → EN+AR — это **наполнение данными и включение флага**, без рефакторинга вёрстки и схемы.

---

## 4. Публичный флоу GRABZI: каталог, создание заказа, мои заказы

Логику **каталога берём из Juicy**, но публичный UX упрощаем под GRABZI: без корзины, без обязательной деталки, без добавок и купонов.

### 4.1. Главная
- Hero/бренд GRABZI + кнопка **«Create order»**. Клик → **сначала выбор локации** (§5.7), затем страница создания заказа.

### 4.2. Создание заказа — одна страница (Р3.1)
Единый экран (аналог `order.grabzi.ae`, но мультилокационный):
- Сверху — выбранная локация + индикатор **остатка** («N left today» или «No limit») и статус часов.
- **Табы категорий напитков**; при выборе категории **сразу виден список всех её напитков** на этой же странице (статичный PNG, имя с эмодзи, цена), у каждого **`+/–` счётчик количества**.
- Снизу — сводка: позиции, сумма; поля **телефон** и **номер авто** (предзаполняются из профиля/прошлых заказов, если есть); кнопка **Pay**.
- **Контроль лимита в реальном времени:** если суммарное число напитков в заказе превышает текущий `remaining` локации — показываем **модалку/сообщение** «Only N drinks left at this location» и блокируем оплату, пока пользователь не уменьшит количество (или не сменит локацию). Остаток обновляется (WS/поллинг §5.7).
- Без добавок; цена позиции = `base_price × qty`.

### 4.3. Деталка напитка — опциональный модуль (Р3.2)
Не обязателен (флаг `product_detail_enabled`, по умолчанию off). Если включён — отдельный экран `/product/[slug]` (как в основном проекте): медиа, описание, KБЖУ самого напитка, счётчик количества и кнопка **«Create order»**, которая ведёт в §4.2 с предзаполнением (этот напиток + qty, телефон, авто) и показом доступного лимита.

### 4.4. Мои заказы (Р10)
Экран `/orders`:
- **Авторизован** → список его заказов (номер, состав, статус, сумма, оценка).
- **Не авторизован** → сообщение «Create your first order or sign in», и форма **ввода номера телефона** → авто-логин по телефону без OTP (§4.7) → показываем историю заказов этого пользователя.

### 4.5. Профиль (Р11)
Личный кабинет как в основном Juicy: **имя**, авто/эмират, настройки. **Без купонов**, без переключателя языка (EN-only).

### 4.6. Бэкенд каталога/заказа (переиспользование Juicy, минимум изменений)
- Каталог: модели/эндпоинты `categories`, `drinks` Juicy — **как есть**.
- Добавки (`Addon/AddonCategory/DrinkAddon/OrderItemAddon`) и купоны (`Coupon`) — **дремлющие**: в GRABZI-флоу не используются (флаги `catalog_addons_enabled=false`, `coupons_enabled=false`). Заказ создаётся без addon-строк и без выпуска купонов. Никаких удалений схемы — чистое переиспользование.
- `POST /drinks/{slug}/preview` на GRABZI не вызывается; цена = `base_price × qty`.
- **Оценка** (`rate`) остаётся, но при `coupons_enabled=false` дизлайк **не выпускает купон** (§Р8).

**Админ-каталог (админка Juicy, для GRABZI):**
- Активны **Categories** и **Products**.
- **Addons/Groups/Units** и **Coupons** — в навигации GRABZI не показываем (код Juicy остаётся, маршруты живут).
- В редакторе напитка скрыт блок «привязка добавок».

### 4.7. Авторизация по телефону без OTP (авто-логин)
**Факт по коду Juicy:** `POST /api/orders` **требует авторизации** (`Depends(get_current_user)`), а `Order.user_id` — **NOT NULL**. Сохраняем это требование, но делаем вход **бесшовным по номеру телефона, без OTP и без пароля** (быстрый drive-through). OTP сейчас **не нужен** (остаётся в коде Juicy за выключенным флагом `auth_otp_enabled=false`).

**Как работает (переиспользуем `auth/verify` Juicy, выключив проверку кода):**
- На странице заказа (§4.2) пользователь вводит **телефон + авто**. При «Pay»:
  - **телефон новый** → создаём пользователя по этому номеру (без пароля/кода), сразу **выдаём токен** (авто-логин) и привязываем заказ к нему;
  - **телефон уже был** → находим существующего пользователя с этим номером, **авторизуем его** (выдаём токен) и привязываем новый заказ к нему — так все заказы с этим телефоном собираются у одного юзера.
- `user_id` всегда проставлен (NOT NULL соблюдён), схема Juicy не меняется. По сути это `auth/verify` Juicy в режиме «без кода»: find-or-create по телефону + токен.

**«Мои заказы» (§4.4):**
- Не авторизован → вводит свой номер телефона → тем же механизмом (find-or-create + токен) **авторизуется и видит свою историю** заказов по этому номеру. Без OTP.

> **Известный компромисс (сейчас осознанно принят):** без OTP вход по одному номеру не подтверждает владение им — теоретически можно ввести чужой номер и увидеть его историю. Для текущего drive-through-MVP это допустимо. **Усиление на будущее:** включить `auth_otp_enabled=true` (код в Juicy уже есть) — тогда вход/история потребуют подтверждение номера кодом. Зафиксировано в §12.

---

## 5. Локации: дневной лимит + рабочие часы (ядро нового функционала)

### 5.1. Модель данных
**`locations`**
| Поле | Тип | Назначение |
|---|---|---|
| `id` | PK | |
| `name` | JSON i18n `{en[,ar]}` | название точки (заполняем `en`; §3.3) |
| `description` | JSON i18n `{en[,ar]}` | описание точки (для публичной карточки/деталки) |
| `address` | str | адрес |
| `coordinates` | json `{lat,lng}` | гео (сортировка по расстоянию, карта) |
| `working_hours` | json | **время открытия/закрытия по каждому дню недели** (см. §5.5) |
| `timezone` | str | TZ границы «дня» (дефолт `Asia/Dubai`) |
| `daily_drink_limit` | int **nullable** | лимит напитков/день; **`null` = без лимита** (Р5.1) |
| `accepting_orders` | bool (default true) | **операционная пауза** приёма заказов (§5.11.3), отдельно от расписания и `is_active` |
| `color` | str | фон карточки |
| `image_url` | str **nullable** | фото точки для карточки (опц., загрузка §5.11.2) |
| `is_active` | bool | вкл/выкл точку (скрывает из публичного списка) |
| `sort` | int | порядок |

**`location_daily_counters`** — счётчик-на-день для атомарной проверки
| Поле | Тип | Назначение |
|---|---|---|
| `id` | PK | |
| `location_id` | FK locations | |
| `date` | date | день в TZ локации |
| `committed_drinks` | int | напитков уже продано (оплачено) за день |
| уник. индекс | (`location_id`,`date`) | одна строка на точку-день |

**`orders`** (переиспользуем модель Juicy) — изменения:
- добавить `location_id` (FK locations). Миграция: **nullable** на старте; бэкфилл исторических заказов сид-локацией `Legacy`; для новых — обязателен (валидация на API).
- **новый статус НЕ вводим.** В Juicy у заказа уже есть два независимых поля: `status` (`new|in_progress|ready|completed|refund` — стадия готовки) и `payment_status` (`pending|paid|failed|refunded`). Менеджерский список (`admin_orders`) **уже фильтрует `payment_status == "paid"`** — то есть неоплаченный заказ (`payment_status="pending"`) **и так невидим менеджеру и в статистике**. Поэтому «видимость только после оплаты» (Р5) достигается **существующим `payment_status`**, без новой сущности и нового значения статуса. Заказ создаётся с `status="new"`, `payment_status="pending"`; при оплате `mark_paid` ставит `payment_status="paid"` + списывает лимит (§5.3).
- зависшие неоплаченные (`payment_status="pending"` старше TTL) — фоновая чистка в `payment_status="failed"` (значение уже есть); они и так скрыты, чистка лишь убирает «висяки».

> Это **максимальное переиспользование**: модель Juicy уже разделяет готовку и оплату, а менеджерский фильтр по `paid` даёт гарантию «нет мусорных заказов» из коробки. Нам остаётся добавить только `location_id` и списание лимита в `mark_paid`.

**`info_blocks`** (для §6) — `key`, `title` (JSON i18n), `body` (JSON i18n, richtext), `sort`, `is_active`.

### 5.2. Логика остатка (с учётом «без лимита», Р5.1)
```
sold_today(loc)   = counter[loc, today(loc.tz)].committed_drinks   (0 если строки нет)
если loc.daily_drink_limit == null:        # лимита нет
    remaining(loc)   = null (∞);  is_sold_out(loc) = false
иначе:
    remaining(loc)   = max(0, loc.daily_drink_limit - sold_today(loc))
    is_sold_out(loc) = remaining(loc) == 0
```
Граница суток — **в TZ локации**, не в UTC. **`committed_drinks` инкрементируется всегда** (даже при `null`-лимите) — чтобы супер-админ видел объём по точке в аналитике (§5.9). Проверка лимита при оплате (§5.3) **пропускается**, если лимит `null`.

### 5.3. Поток оплаты: заказ виден менеджеру и списывает лимит только после оплаты (Р5)
Используем **существующий `payment_status`** Juicy (см. §5.1): неоплаченный заказ (`pending`) уже невидим менеджеру (фильтр `paid`) и не в статистике; лимит списываем в момент оплаты.

Поток (как в Juicy + `location_id` + списание лимита):
1. **«Pay»** — `POST /api/orders` (тело Juicy + `location_id`). Сервер:
   - **мягкая предпроверка**: точка существует, `is_open`, и (если лимит задан) `qty ≤ remaining`; иначе `409` (`LOCATION_CLOSED` / `LOCATION_LIMIT_EXCEEDED` / `LOCATION_SOLD_OUT`) — до создания платежа;
   - создаёт заказ `status="new"`, `payment_status="pending"` (snapshot позиций/цен, `location_id`), **лимит НЕ списывает** (заказ и так скрыт фильтром `paid`).
2. **`POST /api/payments/checkout-session`** (как в Juicy) — Stripe-сессия с `order_id` в `metadata`; возвращает checkout URL (или mock-подтверждение в dev). Оплата — Stripe/Apple Pay.
3. **Вебхук `checkout.session.completed`** (`mark_paid(order_id)`) — **в одной транзакции**:
   1. `SELECT ... FOR UPDATE` строки `counter[location_id, today(tz)]` (создать с 0) — блок строки;
   2. перепроверка `is_open`; и, **если лимит задан**, `committed_drinks + drinks ≤ daily_drink_limit` (при `null` — пропуск);
   3. **успех** → `payment(succeeded)`, **`payment_status="paid"`** (теперь заказ виден менеджеру и в статистике), `committed_drinks += drinks`, события `paid`; коммит;
   4. **провал лимита/часов** (редкая гонка на последние слоты) → `payment_status="refunded"` (остаётся скрытым), **возврат Stripe**, лог `limit_exceeded_refund`; пользователю — «Sold out / closed, refunded».
4. **Idempotency:** по `order_id`/`session.id` повторный вебхук не списывает лимит дважды.
5. **Уборка зависших** (`payment_status="pending"` старше TTL) — фон в `payment_status="failed"` (значение уже есть в Juicy).

> Гарантия: лимит уменьшается и заказ появляется у менеджера **строго после получения денег** — на существующем механизме `payment_status` Juicy, минимум нового кода. До оплаты `remaining` на экране — справочный (без резерва).

### 5.4. Возвраты и лимит (Р5)
При `refund` оплаченного заказа: под тем же row-lock `committed_drinks -= drinks` для дня, **если** возврат происходит в тот же локальный день, что и продажа (иначе лимит прошлого дня не трогаем — он уже «сгорел»). Событие `refund` уже есть в Juicy; добавляем декремент счётчика.

### 5.5. Время открытия/закрытия по дням недели → блокировка заказа (Р6)
Супер-админ задаёт для каждой локации **время открытия и закрытия отдельно по каждому дню недели** (или помечает день выходным). Это (а) влияет на возможность создать заказ в конкретный момент и (б) выводится на публичной странице списка локаций.

- **Формат `working_hours`** — объект по 7 дням, у каждого дня `open`/`close` (или несколько интервалов, или `closed`):
  ```json
  {
    "mon": [{"open":"05:30","close":"22:00"}],
    "tue": [{"open":"05:30","close":"22:00"}],
    "wed": [{"open":"05:30","close":"22:00"}],
    "thu": [{"open":"05:30","close":"22:00"}],
    "fri": [{"open":"05:30","close":"22:00"}],
    "sat": [{"open":"05:30","close":"22:00"}],
    "sun": [{"open":"10:00","close":"18:00"}]
  }
  ```
  Массив интервалов поддерживает перерывы в течение дня; пустой массив `[]` = выходной. Дефолт-сид — из спарсенных часов GRABZI.
- **Редактор в админке (§5.8):** строка на каждый день недели с полями «открытие»/«закрытие» и чекбоксом «выходной»; кнопка «применить ко всем дням».
- `schedule_open(loc, now)` и `next_open_at` — вычисляются в `loc.timezone`.
- **Итоговая доступность точки:** `is_open = is_active && accepting_orders && schedule_open(now)` (учитывает и расписание, и ручную паузу §5.11.3).
- **Публичный справочный API** `GET /api/locations` возвращает `working_hours`, `accepting_orders`, `is_open`, `next_open_at` для вывода на странице локаций.
- В `create_order`/`mark_paid`: точка закрыта по расписанию → **HTTP 409** `LOCATION_CLOSED {next_open_at}`; на ручной паузе → **409** `LOCATION_PAUSED`.
- Итоговое условие прохождения заказа: `is_open && (remaining == null || remaining >= drinks_in_order)`.

### 5.6. API — контракты
**Публичное** (locale-параметр опционален, дефолт `en`; §3.3)
```
GET /api/locations?locale=en
→ 200 [{ id, name, address, coordinates, working_hours, timezone,
         daily_drink_limit, remaining_today, is_sold_out, is_open, next_open_at, color }]

GET /api/locations/{id}        → 200 (тот же объект)

// Авто-логин по телефону без OTP (§4.7): find-or-create user + токен. Переиспользуем auth/verify Juicy с auth_otp_enabled=false:
POST /api/auth/verify          (phone[, name])  → { token, user, created }   // код не требуется

POST /api/orders               (items[], location_id: required, contacts{phone,car})
  // требует токен (получен авто-логином выше); создаёт заказ payment_status="pending"; лимит НЕ списывает
→ 201 { order_id, payment_status: "pending" }
→ 409 { code: "LOCATION_CLOSED",        next_open_at }
→ 409 { code: "LOCATION_LIMIT_EXCEEDED", remaining, requested }
→ 409 { code: "LOCATION_SOLD_OUT" }
→ 422 { code: "LOCATION_REQUIRED" }            // location_id не передан

POST /api/payments/checkout-session  (order_id)   // как в Juicy → { checkout_url } | dev mock
GET  /api/orders/{id}          → 200 order        // владельцу (по токену)
GET  /api/orders               → 200 [order]      // история текущего пользователя (после авто-логина)
```
**Платёж / финализация (лимит и видимость менеджеру — здесь)**
```
POST /api/payments/webhook  (checkout.session.completed → mark_paid(order_id)):
  - row-lock counter[location, today]; перепроверка is_open + лимита;
  - успех → payment.succeeded + payment_status="paid" + committed_drinks += drinks (одна транзакция);
  - провал → заказ в cancelled/payment_refunded (скрыт), авто-refund Stripe, лог limit_exceeded_refund;
  - idempotent по order_id / session.id.
```
**Админка (super_admin)**
```
GET    /api/admin/locations                 → список + sold_today/remaining/is_open/accepting_orders
POST   /api/admin/locations                 (name, description, address, coordinates, image_url, working_hours, timezone, daily_drink_limit, accepting_orders, color, is_active)
PATCH  /api/admin/locations/{id}            (любые поля выше)
POST   /api/admin/locations/{id}/pause      { accepting_orders: bool }   // пауза/возобновление (§5.11.3); manager — только своя точка
GET    /api/admin/locations/{id}/daily?date=YYYY-MM-DD   → { committed_drinks, remaining, orders[] }
POST   /api/admin/locations/{id}/adjust-day { date, delta | set_committed }   // ручная корректировка

# Контент сайта (CMS, §5.11.1)
GET    /api/admin/content                   → [info_blocks]
POST   /api/admin/content                   (key, title, body, sort, is_active)
PATCH  /api/admin/content/{id}              (любые поля)
GET    /api/content?locale=en               → публичный контент инфо-страницы/блоков

# Загрузка медиа (§5.13) — нового в Juicy нет
POST   /api/admin/media   (multipart file)  → { url }   // в объектное хранилище; URL пишем в drink/category/location

# Дашборд (дорабатываем Juicy: + группировка по локациям)
GET    /api/admin/dashboard?from=&to=&location_id=   → метрики + разрез по локациям (§5.10)
```

### 5.7. UI — публичная часть
- **Выбор локации** (`/outlets`, класс B — новый из Juicy на GRABZI-ките): карточки точек, на каждой `RemainingBadge` «N left today» и `LocationStatusBadge`; `is_sold_out` или `!is_open` → карточка-disabled (заказ недоступен, текст «Sold out» / «Closed · opens 5:30»). desktop — сетка карточек/карта, mobile — список.
- **CTA «Create order»** (главная, header desktop): локация не выбрана → выбор локации → затем страница создания заказа (§4.2). Выбранная локация хранится в сторе (persist).
- **Индикатор остатка** постоянно на странице заказа (desktop header + mobile top bar).
- **Страница заказа (§4.2):** суммарное `qty > remaining` или `!is_open` → **модалка** «Only N drinks left» + блок кнопки Pay, пока не уменьшат количество/сменят локацию.
- **Ошибки оплаты:** 409 `LOCATION_LIMIT_EXCEEDED`/`LOCATION_CLOSED` → экран/тост с объяснением и кнопкой «Change location».
- **Реалтайм остатка:** WebSocket-канал `ws/locations` (broadcast при `mark_paid`/`refund`), фолбэк — поллинг `GET /api/locations` каждые N сек на экране выбора/каталога.

### 5.8. UI — админка (раздел «Locations», класс B на Juicy-админ-ките)
- `/admin/locations` — таблица: name, address, **limit/day (или «No limit»)**, **sold today / remaining**, is_open, **accepting/paused**, status; кнопки «New location», **«Pause/Resume»** на строке.
- Модалка/форма локации: **name** (EN), **description**, **address**, coordinates, **фото точки** (загрузка §5.13), **working_hours — редактор открытия/закрытия по каждому дню недели** (§5.5), timezone, **daily_drink_limit (пусто = без лимита)**, **accepting_orders (пауза)**, color, is_active.
- `/admin/locations/[id]` — карточка: редактирование; график sold/remaining по дням (исторический, `daily?date=`); кнопка ручной корректировки дня (`adjust-day`); кнопка **«Pause/Resume orders»**; **список закреплённых сотрудников** (§5.9).
- Пункт «Locations» в сайдбаре `AdminShell`.

### 5.9. Сотрудники, привязка к локации и доступ менеджера (Р13)
**Модель.** В `staff_users` добавляем `location_id` (FK locations, **nullable**). Семантика по ролям:
- `manager` — **обязан** иметь `location_id`; работает только со своей точкой;
- `super_admin` — `location_id = null` (видит все точки).

**Создание/редактирование сотрудника (супер-админ).** В `/admin/staff` форма сотрудника получает поле **«Location»** (select из активных локаций). Супер-админ прикрепляет/меняет точку менеджера. Валидация: для роли `manager` локация обязательна.

**Контроль доступа (ключевое — чтобы менеджер не трогал чужие заказы).**
- Бэкенд: во **всех** админ-эндпоинтах заказов (`GET /api/admin/orders`, `/orders/{id}`, `take`, `status`, `refund`) для роли `manager` **жёстко фильтруем/проверяем `order.location_id == staff.location_id`**. Попытка открыть/взять чужой заказ → **403 `FOREIGN_LOCATION`** (фильтрация на уровне запроса, не только UI).
- Заказ относится к точке через `order.location_id` (фиксируется при создании, Р5).
- Realtime-канал админки `ws/admin/orders` для менеджера подписывает только его `location_id`.
- Фронт `/admin/orders`: менеджер видит заголовок «Location: <name>» и список **только своей точки**, без переключателя точек; супер-админ — все точки + фильтр по локации.

**API.**
```
POST  /api/staff/managers            (… + location_id)      # super_admin
PATCH /api/staff/managers/{id}       (… + location_id)      # super_admin: сменить точку
GET   /api/admin/orders?location_id= (super_admin: фильтр; manager: игнор, всегда своя точка)
```

### 5.10. Аналитика по локациям (дашборд, Р13 + сравнение лимитов)
Супер-админ должен **сравнивать точки** и видеть, **как расходуется лимит** по каждой.
- **Дашборд `/admin` (super_admin):** разрез метрик **по локациям** — выручка, заказы, напитки; колонка/виджет **«sold today / limit / remaining / % выработки»** по каждой точке рядом для сравнения; «No limit» для точек без лимита.
- **Локация менеджера видна в аналитике:** в списках сотрудников/заказов и в разрезах указывается, **за какой точкой закреплён менеджер** (кто из персонала на какой локации).
- **Менеджер на дашборде** (если доступен) видит метрики **только своей точки**.
- **На рабочем экране заказов** (`/admin/orders`) менеджер видит мини-панель своей точки: **sold today / remaining / limit**, статус «open/closed/paused» — чтобы понимать, сколько ещё можно принять (закрывает UX-пробел).
- Данные берём из `orders.location_id` (исторично) и `location_daily_counters` (по дням). **Дашборд-эндпоинт Juicy дорабатываем:** добавляем группировку/фильтр `?location_id=` (сейчас его нет) и блок сравнения точек.

### 5.11. Управление контентом, медиа и операционные настройки в админке
> Закрывает пробелы аудита Juicy-админки: нет загрузки медиа, нет CMS контента, нет операционной паузы точки, нет экрана настроек.

**5.11.1. Контент сайта (CMS) — раздел `/admin/content` (в скоупе, не опционально).**
Управляем редактируемым контентом публичного GRABZI без релиза, через таблицу `info_blocks`:
- блоки инфо-страницы (§6): **Our Story** (текст), **Contact us** (email, телефон, **соцсети** — список ссылок), подпись/тексты раздела Location;
- произвольные текстовые блоки (например, баннер/слоган на главной).
- Поля блока: `key`, `title` (JSON i18n), `body` (richtext, JSON i18n), `sort`, `is_active`.
- API: `GET/POST/PATCH /api/admin/content`, публичный `GET /api/content?locale=`.
- **Главная (hero/секции)** в v1 — **hardcode из конфигурации темы GRABZI** (решено §12.13, не редактируется из админки); CMS-hero (`info_blocks` тип `hero`) — только при будущей необходимости.

**5.11.2. Загрузка медиа напитков/категорий — новый аплоад (важно, в Juicy его нет).**
В Juicy медиа хранится **только как URL-строка**, файл-аплоада нет — для GRABZI добавляем:
- эндпоинт `POST /api/admin/media` (multipart) → загрузка в объектное хранилище (S3-совместимое), возвращает URL; этот URL пишется в `drinks.preview_url`/`video_url`, `drink_categories.*`, опц. `locations.image_url`.
- В редакторе напитка/категории/локации — поле загрузки файла (фото/видео-петля) вместо ручного URL (URL тоже оставляем как фолбэк).
- Видео-петли карточек GRABZI — загружаются здесь же.
> **Решено (§12.12):** заводим объектное хранилище и аплоад из админки (URL-only-фолбэк не выбран).

**5.11.3. Операционная пауза точки — только super_admin** (детально §5.14).
Ручная пауза приёма заказов (отдельно от `is_active` и расписания), поле `locations.accepting_orders`. **Пересмотр:** управление паузой — **исключительно super_admin** (менеджер не должен иметь рычаг «выключить точку»; риск злоупотребления). Менеджер видит статус своей точки read-only (§5.16). Опциональная история событий точки — §5.14.

**5.11.4. Стоп-лист напитка по локации — ДЕЛАЕМ** (детально §5.15).
**Пересмотр Р7:** делаем операционный стоп «кончилось здесь» (таблица `location_drink_stops`), отдельной осью от глобального `Drink.status=hidden`. Управляет менеджер своей точки + super_admin. Не матрица видимости — лёгкий рантайм-оверлей доступности.

**5.11.5. Экран настроек — ДЕЛАЕМ** (детально §5.17).
**Пересмотр:** `/admin/settings` (только super_admin) с чётким разделением **global (`app_settings`) vs per-location (`locations`) vs env (read-only)** — чтобы разделы не путались.

> Детальные, заземлённые в коде проекты §5.11.2–5.11.5 раскрыты ниже (§5.12–§5.17). Предварительно — §5.12 фиксирует общие предусловия и реалии кодовой базы, без которых эти доработки не лягут чисто.

---

## 5A. Детальный дизайн доработок админки (заземлён в коде Juicy)

### 5.12. Фундамент локаций и реалии кодовой базы (предусловие §5.13–§5.17)
> Адверсариальная ревизия выявила сквозные предусловия — без них пять доработок конфликтуют между собой и с кодом. Фиксируем их как **общую первую задачу**.

- **Замещаем существующий мок `/admin/outlets`, а не плодим второй раздел.** В Juicy уже есть `app/app/admin/outlets/page.tsx` + `[id]/page.tsx` + `app/lib/admin-mock.ts` (матрица Меню×Точки, кнопка «Закрыть точку», `outletScope` сотрудников). Раздел **«Точки/Locations» замещает их**: эволюционируем эти страницы на реальный API (переиспользуем крошки/табы) **или** удаляем outlets-мок и данные `admin-mock`. **DoD: в админке остаётся ОДНО дерево точек**, мок удалён/мигрирован.
- **Единый «фундамент локаций» — одна задача, один владелец (Фаза 3):** `Location` (+ `LocationDailyCounter`), `StaffUser.location_id`, `Order.location_id`, **`OrderIn.locationId`** (`orders.py`), и проброс `locationId` в **staff `/me` payload + Staff TS-тип** (`adminApi.ts`) — иначе фронтовый скоуп менеджера молча не работает (токен несёт только `sub/kind/role`). Всё §5.13–5.17 это **только потребляют**.
- **Миграции: в проекте НЕТ Alembic.** `main.py` использует `Base.metadata.create_all` (явное решение «Alembic до первого изменения схемы в проде»). `create_all` создаёт только отсутствующие таблицы — **ALTER существующих не делает**, поэтому новые колонки (`Order.location_id`, `StaffUser.location_id`, `locations.accepting_orders`) на засеянной БД **не применятся**. Решение: **вводим Alembic сейчас** (это и есть то самое первое изменение схемы) — `alembic init` + первая ревизия со всеми новыми таблицами/колонками. Для dev допустим разрушительный reset (`drop juicy.db`/volume). Внести в DoD каждой фазы со схемой.
- **SQLite FK PRAGMA.** Дефолтная БД — SQLite, `db.py` не включает `PRAGMA foreign_keys=ON` → `ON DELETE CASCADE` (стоп-лист, история) молча игнорируется в dev. Включить connection-event-listener `PRAGMA foreign_keys=ON`, либо не заявлять БД-целостность и чистить в коде.
- **Один роутер `backend/app/routers/admin_locations.py` с ПЕР-ЭНДПОИНТНЫМИ гардами** (НЕ роутер-уровневый `require_super_admin`): `PATCH`/`pause`/`history`/настройки — `super_admin`; `stops`/`location-status` — `require_manager_or_super` + scope «своя точка». Три дизайна целились в один файл — собираем в один модуль, один `include_router`.
- **Счётчик лимита — НЕ внутри `notify()`.** `notify(order)` чист (только publish), не имеет ни `db`, ни `location_id`. Инкремент — **явным шагом в `mark_paid`** (после commit), декремент — в `order_flow.refund_order` (переносим инлайновый refund из `admin_orders.py` в `order_flow`, чтобы он проходил через ту же точку и слал `notify` с `location_id`). **Лимит-гейт** (`409 LOCATION_LIMIT_REACHED`) добавляется в ту же helper-доступности, что `LOCATION_PAUSED`/`LOCATION_CLOSED` (§5.3 это уже предполагал — здесь фиксируем место).
- **Расписание — одно имя поля: `working_hours`** (не `schedule`); `schedule_open`/`next_open_at` читают его.
- **Статус точки — НЕ хранимая enum-колонка.** Единственный ручной тумблер — `accepting_orders` (bool). Презентационный статус `open/paused/closed` **вычисляется на чтении** из `is_active + accepting_orders + schedule_open(now)`. Панель менеджера (§5.16) потребляет вычисленный статус, своей `Location.status` не вводит.
- **Единый WS-канал `location:{id}`** для статуса/паузы/стопа/счётчика (а не три разных) — фронту одна подписка на панель точки.

### 5.13. Медиа: локальный S3 (MinIO) + drag-and-drop загрузка везде
> §12.12 решено: аплоад в объектное хранилище. В Juicy медиа — только URL-строка, файл-аплоада нет. Добавляем **один** storage-слой, **один** аплоад-эндпоинт, **один** drag-and-drop компонент на всю админку.

- **docker-compose:** сервисы `minio` (`minio/minio`, порты 9000 API / 9001 console, том `miniodata`) + одноразовый `minio-init` (`minio/mc`: создаёт бакет `grabzi-media`, делает префикс `media/` публичным на чтение). `backend` получает блок env и `depends_on: minio`.
- **Конфиг:** `requirements.txt += boto3` (синхронный, под текущий sync-стек; `python-multipart` уже есть). `config.py += s3_endpoint_url/s3_public_url/s3_access_key/s3_secret_key/s3_bucket/s3_region` (все опциональны; без `s3_bucket` аплоад → `503 MEDIA_STORAGE_DISABLED`, остальное работает). Две раздельные переменные адреса: `S3_ENDPOINT_URL` (запись изнутри docker `minio:9000`) и `S3_PUBLIC_URL` (ссылка для браузера/CDN) — убирает костыль «внутренний хост в публичной ссылке».
- **Storage-слой `backend/app/services/storage.py`** — единственное место про boto3: `upload(file, folder)` (валидация MIME jpeg/png/webp/mp4/webm + размер ≤25МБ, ключ `media/{folder}/{uuid}{ext}` — имя файла пользователя не используется → нет path-traversal), `delete(key)`, `media_url(key)`.
- **Хранение ссылки (фикс ревизии — без ложного обещания «смена провайдера = только env»):** в строковых полях БД храним **относительный ключ/путь** (`media/drinks/ab12.mp4`), а абсолютный URL собираем **на чтении** одним хелпером `media_url(key)` (база — `S3_PUBLIC_URL`). Это даёт честный provider-swap **без переписывания строк в БД** и единообразно работает с уже существующими относительными путями сидов (`/videos/*.mp4`). Поля остаются `String(300)`, новая «media»-таблица не нужна.
- **Эндпоинт** `POST /api/admin/media` (multipart) в новом `admin_media.py`, гард `require_super_admin` (как весь `/api/admin/catalog`). Возвращает `{ key, url, contentType, size }`. Существующие JSON-эндпоинты каталога **не меняем** — фронт кладёт ключ в то же поле обычным PATCH.
- **Фронт — один компонент** `app/components/admin/MediaUploadField.tsx` (drag-and-drop + клик, превью img/video-loop, прогресс через XHR, кнопка очистки, URL-инпут как фолбэк). Заменяет ВСЕ текстовые URL-инпуты медиа: `products/[slug]` (`previewUrl`,`videoUrl`), `categories` (`photoUrl`,`videoUrl`), `addons` (`imageUrl`,`iconUrl`), опц. `locations.image_url`. `adminApi.ts += uploadMedia()` (FormData+XHR-прогресс; общий парсер ошибки `safeDetail` вынести из `req()` и переиспользовать — DRY). CSS-классы dropzone в `admin.css`.
- **Роль:** загрузка медиа — **super_admin** (каталог и так super-only). На manager-видимом экране локации поле image_url у менеджера отсутствует/disabled.
- **Поддерживаемость:** один сервис знает про S3; MinIO→AWS/R2/Yandex — смена env, без кода/фронта; приватный режим — заменить `media_url` на presigned-GET (одна функция); очистка осиротевших файлов — `storage.delete(key)` (ключ уже в поле), фоновым GC позже.

### 5.14. Операционная пауза точки (super_admin) + опциональная история
- **Поле** `locations.accepting_orders` (bool, default true) — единственный ручной тумблер. Доступность: `is_open = is_active AND accepting_orders AND schedule_open(now)` (один множитель в существующей формуле, без дублирования логики).
- **Коды 409 различаем:** расписание → `LOCATION_CLOSED {next_open_at}`; ручная пауза → `LOCATION_PAUSED {}` (бессрочна, без `next_open_at`). Проверка — в той же helper-доступности (порядок: `is_active`→`accepting_orders`→`schedule_open`).
- **Эндпоинт** `POST /api/admin/locations/{id}/pause` `{accepting_orders, reason?}` — **`require_super_admin`** (пер-эндпоинтный Depends; менеджер с JWT → 403). Идемпотентно (повтор того же состояния — no-op). Отдельно от `PATCH /locations/{id}` (полное редактирование). WS-публикация в `location:{id}`.
- **(ОПЦИОНАЛЬНО) История** `location_status_events` по точному паттерну `OrderEvent` (`location_id`, `action` `pause|open`, `by_staff_id`, `reason`, `created_at`; `cascade=all,delete-orphan`). Длительность паузы — **вычисляется на чтении** (интервал `pause`→следующий `open`), не хранится. `GET /api/admin/locations/{id}/history` (super_admin) → `[{action, byStaffName, reason, at, durationMinutes|null}]`. UI — таймлайн копией таймлайна заказа (`admin-timeline*`). Базовая пауза работает **без** этой таблицы; блок включаемый.
- **UI:** в сайдбаре «Точки» (`roles:['super_admin']`); на строке — «Поставить на паузу»/«Возобновить приём» + бейдж «На паузе». Кнопки рендерятся только для super_admin. Авто-возобновление по таймеру — вне скоупа (пауза снимается вручную).

### 5.15. Стоп-лист напитка по локации
- **Модель** `location_drink_stops` (`backend/app/models/catalog.py`): `location_id FK→locations ON DELETE CASCADE`, `drink_id FK→drinks ON DELETE CASCADE`, `reason String(200)?`, `by_staff_id FK→staff_users?`, `created_at`; `UniqueConstraint(location_id, drink_id)`. **Стоп = наличие строки**, снятие = hard delete. (Аудит «кто/когда» — событие `drink_stopped/drink_resumed` в журнале точки §5.14, отдельной колонкой `drink_id`, не в `note`.) `drink_id`/`location_id` — настоящие FK (не Integer-снэпшот, как `OrderItem.drink_id`).
- **Две независимые оси** (не дублируют, не мутируют друг друга): глобальный `Drink.status=hidden` (нет нигде, `DRINK_NOT_AVAILABLE`) vs локальный стоп (`DRINK_UNAVAILABLE_AT_LOCATION`, в каталоге «sold out»). Порядок: сначала глобальный статус, потом локальный стоп. **Р7 не нарушаем** — это рантайм-оверлей поверх единого published-каталога, не матрица-копия каталога на точку.
- **API** (поддерево `/{location_id}/stops` в `admin_locations.py`): `POST {drinkId, reason?}` (идемпотентно), `DELETE /{drink_id}` (идемпотентно, нет строки → 204), `GET` список. Гард `require_manager_or_super` + **scope «своя точка»** (manager и `staff.location_id != location_id` → `403 NOT_YOUR_LOCATION`). После мутации — событие + WS `location:{id}`.
- **Роль:** super_admin — любая точка; **manager — своя точка** (стоп «кончилось» происходит десятки раз в день за стойкой; запирать на super_admin = неактуальный каталог). Контраст с паузой ВСЕЙ точки (super_admin only) — стоп одного напитка низкорисковый и обратимый.
- **Применение №1 (каталог):** `catalog.py` `list_drinks/drink_detail/drink_preview` принимают `location_id: int|None`. Helper `stopped_drink_ids(db, location_id) -> set[int]` (один запрос). `list_drinks` НЕ скрывает напиток, а помечает `soldOut: true` (скрытие = плохой UX). **Фикс ревизии:** контракт `drink_preview` (источник цены) НЕ ослабляем — `soldOut` добавляем только в payload `list_drinks/drink_detail` через `stopped_drink_ids`, не трогая 404-логику preview. Фронт `api.ts` пробрасывает `location_id`, `ApiDrinkLite += soldOut?`, карточка гасит степпер.
- **Применение №2 (заказ):** стоп-проверка `DRINK_UNAVAILABLE_AT_LOCATION` — в `create_order` (**до/независимо** от `drink_preview`, чтобы ослабление detail-страницы не утекло в ценообразование) и повторно в `mark_paid` (товар мог кончиться за время чекаута). `order.location_id` из `OrderIn` (§5.12).
- **UI:** на `/admin/locations/[id]` секция «Стоп-лист» (тоггл В продаже/Стоп по каждому напитку + reason) для super_admin; быстрый тоггл-чипы на рабочем экране менеджера рядом с панелью остатка (бариста снимает в 2 тапа). WS `location:{id}` обновляет realtime.

### 5.16. Остаток/лимит точки на рабочем экране менеджера
- **Данные:** `LocationDailyCounter(location_id, business_date, sold_count, updated_at)` — материализованный агрегат (инкремент O(1), без `COUNT(*)`). `business_date` — по `location.timezone` (обнуление в местную полночь, ленивая строка на первый заказ дня, без cron). Инкремент в `mark_paid`, декремент в `order_flow.refund_order` того же дня (§5.12 — НЕ в `notify()`).
- **Эндпоинт** `GET /api/admin/location-status` (`get_current_staff` — доступно обоим): manager → своя точка (по `staff.location_id`); `?location_id=` и `?scope=all` (агрегат Σ) → super_admin. Ответ `{locationId, name, status(вычислен), soldToday, limit, remaining, pausedReason, nextOpenAt, updatedAt}`. Realtime — WS `location:{id}` (событие `counter`), фолбэк поллинг 10с при обрыве.
- **UX-панель** — sticky под топ-баром на `/admin/orders`, над фильтрами; **не дёргает** перезагрузку таблицы заказов. Зоны: статус-индикатор + имя точки | `Sold today N / Limit M · Remaining K` (или `No limit`) + прогресс-бар | live-индикатор. Пороги: <80% синий, ≥80% жёлтый «почти лимит», ≥100% красный «SOLD OUT»; пауза/закрытие → серый. Статус (open/paused/closed) — **вычисленный на чтении** (§5.12), не хранимая enum.
```
┌──────────────────────────────────────────────────────────────────────┐
│ ● Bay Avenue     Sold today 86 / Limit 100 · Remaining 14  ⚠ почти    │
│   Открыта        ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░  86%        ⟳ live · 1 с    │
└──────────────────────────────────────────────────────────────────────┘
```
- **Роль:** видят оба; manager — своя точка read-only (без селектора, без кнопок паузы); super_admin — селектор «Все точки/конкретная» + действия паузы (§5.14). Связь с лимитом: при `sold ≥ limit` сервер реально отклоняет заказ (`409 LOCATION_LIMIT_REACHED`, §5.12), панель не просто информативна.

### 5.17. Экран настроек `/admin/settings` (super_admin) — global vs per-location
> §12.15: делаем, но узко — только **глобальные бизнес-дефолты**. Секреты/инфра — read-only из env. Операционные параметры точки — в `/admin/locations`, **не дублируются**.

- **Три уровня конфигурации:** (1) **env/инфра** (`config.py`, `.env`) — меняет DevOps, в админке read-only; (2) **`app_settings`** (глобальные дефолты) — super_admin на `/admin/settings`; (3) **`locations`** (per-location) — редактируется в «Точки», НЕ здесь. Резолв: per-location → глобальный дефолт → константа.
- **Модель** `app_settings` (типизированный key-value, не «блоб»): `key PK`, `value JSON {"v":...}`, `updated_at`, `updated_by`. Реестр `SETTINGS_SCHEMA` в коде (`settings_registry.py`) — единый источник типов/дефолтов/валидации; новый параметр = строка в реестре, без миграции. `get_setting(db,key)` с lazy-fallback на дефолт (сид не обязателен). **Фикс ревизии:** дефолты контактов (email/phone) в реестре = `None` (реальные значения — через сид-данные, не код-литералами).
- **Что в `app_settings` (Defaults/General):** `default_daily_drink_limit`, `display_currency=AED`, `default_timezone=Asia/Dubai`, `default_locale`, `support_contact_email/phone`, и (если бизнесу нужно в рантайме) тоглы фич. **Фикс ревизии:** глобальные дефолты должны **реально читаться** при создании точки — `POST /api/admin/locations` инициализирует `daily_drink_limit/timezone` из `get_setting(...)`, иначе контрол декоративный. **Фич-флаги — один владелец:** если публичный сайт уже читает фронт-флаг (Р3.2/Р8), НЕ дублируем в `app_settings`; иначе делаем `app_settings` каноном и сайт читает резолв через публичный эндпоинт.
- **API** `GET/PATCH /api/admin/settings` (роутер-уровневый `require_super_admin`). GET → `{editable:{general,defaults}, readonly:{integrations}}`; секреты — только статус `configured|missing`, без значений. PATCH — только editable-ключи (env/integrations → `403 READONLY_SETTING`; неизвестный ключ → `422`).
- **UI** `/admin/settings` (пункт «Настройки» `roles:['super_admin']`), 3 вкладки: **General** (locale, контакты, тоглы), **Defaults** (дефолт-лимит/валюта/TZ + явная подпись «дефолты для новых точек; параметры точки — в разделе Точки» со ссылкой), **Integrations** (read-only карточки Stripe/OTP/JWT/S3/DB/Redis + константы `rating_timeout_minutes`/`locales`). Защита от путаницы global↔per-location: экран явно отсылает в Locations за per-loc-параметрами.

---

## 6. Объединённая инфо-страница (§ Р: open, дефолт `/info`)

Сливаем `Location + Shop Story + Working hours + Contact us` в один экран `/info` с якорным оглавлением. desktop — две колонки (оглавление-стики слева, контент справа), mobile — аккордеон/секции.

1. **Location** — адреса всех точек + статусы/часы из `GET /api/locations` (карта на desktop).
2. **Our Story** — текст бренда (`info_blocks[story]`, дефолт — из спарсенного `story`).
3. **Working hours** — из `working_hours` локаций (а не хардкод).
4. **Contact us** — `grabzi150@gmail.com`, `+971556676679`, соцсети.

Навигация GRABZI: 4 пункта → 1 пункт «Info» с переходами к якорям. Контент секций 2 (Story) и 4 (Contact + соцсети) — `info_blocks`, **редактируется в админке** (раздел «Content», §5.11.1 — в скоупе, не опционально).

---

## 7. Полная карта экранов (GRABZI / Juicy / класс / desktop+mobile)

Легенда: A — воспроизводим как есть (GRABZI); B — добавляем из Juicy на GRABZI-ките; ✅/❌/🟡 — наличие.

### 7.1. Публичный сайт
| Экран | GRABZI | Juicy | Класс / Действие | Адаптив |
|---|---|---|---|---|
| Сплеш-редирект `/` | 🟡 | ✅ | B (минимальный) | оба |
| ~~Онбординг~~ | — | ✅ | **УБРАН (Р9)** | — |
| ~~Ввод имени~~ | — | ✅ | **УБРАН (Р9)** | — |
| ~~Корзина `/cart`~~ | — | ✅ | **УБРАНА (Р3.1) — заменена одностраничным заказом** | — |
| Главная `/home` | ✅ (Shopify) | ✅ | A (бренд GRABZI) + CTA «Create order» | оба |
| **Выбор локации `/outlets`** | ❌ | 🟡 мок | **B + остаток/часы; показывается перед созданием заказа** | оба |
| **Создание заказа `/order`** | 🟡 (их order-app) | 🟡 (cart+checkout) | **B: одна страница — категории+напитки+`+/–`, контакты, Pay (§4.2)** | оба |
| Меню (= `/order`) | 🟡 | ✅ | каталог логикой Juicy, **без добавок**; пункт «Menu» ведёт на `/order` (§12.10) | 2 кол. моб / 3–4 десктоп |
| Деталка `/product/[slug]` | ✅ | ✅ | **опциональный модуль (Р3.2), по умолчанию off** | 1/2 кол. |
| Оплата (Stripe/Apple Pay) | ✅ | ✅ | A; **заказ виден менеджеру/списывает лимит после оплаты (§5.3)** | оба |
| Статус заказа `/orders/[id]` | 🟡 `/success` | ✅ | B (прогресс, «I'm here»); доступен после оплаты | оба |
| «Я на месте» (флаг) | ❌ | ✅ | B | оба |
| Оценка 👍/👎 | ❌ | ✅ | B, **без купона (Р8)** | оба |
| Мои заказы `/orders` | ✅ | ✅ | B; авто-логин по телефону без OTP для истории (Р10, §4.4/§4.7) | оба |
| Вход по телефону `/auth` | ✅ | ✅ | A; авто-логин без OTP (find-or-create, §4.7) | оба |
| Профиль `/profile` | ❌ | ✅ | B; имя/авто/настройки, **без купонов** (Р11), EN-only | оба |
| **Инфо `/info`** | ❌ (4 стр.) | ❌ | **B + объединение** | оба |

### 7.2. Админка (Juicy-кит, без ре-дизайна)
| Раздел | GRABZI | Juicy | Действие |
|---|---|---|---|
| Логин `/admin/login` | ✅ | ✅ | как в Juicy |
| Дашборд `/admin` | 🟡 | ✅ | как в Juicy **+ дорабатываем: группировка `?location_id=`, сравнение лимитов, локация менеджера (§5.10)** |
| Заказы `/admin/orders[/id]` | ✅ `/staff` | ✅ | как в Juicy **+ фильтр `location_id`; менеджер — только своя точка + sticky-панель остатка (§5.16) + быстрый стоп-лист (§5.15)** |
| Покупатели `/admin/customers` | 🟡 | ✅ | как в Juicy |
| Каталог: Categories/Products | ❌ | ✅ | как в Juicy **+ drag-and-drop загрузка медиа (§5.13)** |
| Каталог: Addons/Groups/Units | ❌ | ✅ | **в навигации GRABZI не показываем** |
| **Точки `/admin/locations`** | ❌ (мок `/admin/outlets`) | ❌ | **НОВОЕ (§5.8/§5.14/§5.15): описание/адрес/фото/часы/лимит/пауза(super_admin)/стоп-лист; ЗАМЕЩАЕТ мок `/admin/outlets` (§5.12)** |
| Платежи `/admin/payments` | 🟡 | ✅ | как в Juicy |
| Купоны `/admin/coupons` | ❌ | ✅ | **в навигации GRABZI не показываем (Р8)** |
| Персонал `/admin/staff` | ✅ | ✅ | как в Juicy **+ привязка к локации (`location_id` в форме/`/me`/типе, §5.9/§5.12)** |
| **Content** `/admin/content` | ❌ | ❌ | **НОВОЕ (§5.11.1): Story/Contact/соцсети/блоки** |
| **Настройки** `/admin/settings` | ❌ | ❌ | **НОВОЕ (§5.17, super_admin): global-дефолты; env read-only; per-location — в Точках** |
| **Media-аплоад** `POST /api/admin/media` | ❌ | ❌ (URL-only) | **НОВОЕ (§5.13): MinIO + один drag-and-drop компонент** |

---

## 8. Данные и миграции

- **⚠️ Реальность миграций (фикс ревизии): в проекте НЕТ Alembic** — `main.py` использует `Base.metadata.create_all` (создаёт только отсутствующие таблицы, **ALTER не делает**). Новые колонки к существующим таблицам на засеянной БД **не применятся**. Решение: **вводим Alembic** (первая ревизия со всеми изменениями) либо для dev — разрушительный reset. Включить `PRAGMA foreign_keys=ON` для SQLite (иначе `ON DELETE CASCADE` молча игнорируется). См. §5.12.
- **Новые таблицы/колонки (одна ревизия, §5.12 «фундамент»):** `locations` (`name/description/address/coordinates/working_hours/timezone/daily_drink_limit nullable/accepting_orders/image_url/color/is_active/sort`), `location_daily_counters` (`location_id, business_date, sold_count`, uniq), `location_drink_stops` (§5.15), `location_status_events` (опц., §5.14), `app_settings` (§5.17), `info_blocks` (§5.11.1); колонки `orders.location_id`, **`staff_users.location_id`**, поле `OrderIn.locationId` + `locationId` в staff `/me`/Staff-типе. **Статус заказа НЕ добавляем** — видимость после оплаты на существующем `payment_status` (§5.1).
- **Замещаем мок `/admin/outlets`** (§5.12): удалить/мигрировать `app/app/admin/outlets/**` + outlet-данные `admin-mock.ts` — одно дерево «Точки».
- **Медиа-хранилище (§5.13):** локальный MinIO (docker-compose) для аплоада; в БД — **относительный ключ**, абсолютный URL — на чтении (`media_url(key)`), единообразно с относительными путями сидов.
- **Контент-поля** (`drinks.name/description`, `drink_categories.name`, `locations.name/description`, `info_blocks.*`) — JSON i18n `{en[,ar]}` (заполняем `en`; AR — §3.3).
- **Сиды GRABZI:**
  - Локации: 1–N точек с `working_hours` Пн–Сб 5:30–22:00 / Вс 10:00–18:00, `timezone=Asia/Dubai`, `daily_drink_limit` (напр. 150 — отсылка к «150 в день» из их Story; или `null` = без лимита).
  - Каталог: импорт напитков из `grabzi_parser/output/products.json` (+ из `/pages/menu`: Choco berry 34, Melon spark 37 …) как `drinks` (EN-имена с эмодзи, base_price, медиа). Категории — из меню.
  - `info_blocks`: story (из спарсенного), contacts.
- **Что в GRABZI просто не используем** (код Juicy не удаляем, на фронте GRABZI не выводим): добавки (`Addon*`, `DrinkAddon`, `OrderItemAddon`), купоны (`Coupon`), онбординг, ввод имени, корзина. Деталка напитка — опциональный модуль (Р3.2), на старте не подключаем.

---

## 9. Фазы и критерии приёмки (Definition of Done)

**Фаза 0 — Берём бэкенд Juicy как основу (0.5–1 д).** Поднять проект на коде Juicy (бэкенд+админка) под GRABZI; миграции-доработки и сиды GRABZI из парсера.
*DoD:* бэкенд Juicy запускается под GRABZI; миграции применяются; сиды GRABZI (локации/каталог) заливаются.

**Фаза 1 — Фронт GRABZI: UI-кит + адаптив + дешёвый i18n-задел (3–4 д).** Отдельное публичное приложение GRABZI; шрифты/токены/тема; `GrabziHeader`(desktop)/`GrabziBottomNav`(mobile); §3.2; брейкпоинты. **No-regret §3.3.A:** словарь `messages/en.json` (нет хардкода), **логические CSS везде**, контент JSON i18n. *(Полный AR §3.3.B — отдельный модуль.)*
*DoD:* главная GRABZI на desktop и mobile, подключена к бэкенду; навигация по брейкпоинту; строки через словарь; Lighthouse ≥ 90.

**Фаза 2 — Каталог + одностраничное создание заказа (3–4 д).** Каталог логикой Juicy; **страница `/order` (§4.2): категории+напитки+`+/–`, контакты, Pay**; деталка `/product/[slug]` — за флагом (Р3.2); добавки/купоны скрыты.
*DoD:* выбрал категорию → видны все её напитки со счётчиками; собрал заказ без корзины/добавок; превышение остатка → модалка и блок Pay; e2e «order page → Pay».

**Фаза 3 — Фундамент локаций + Alembic + оплата-then-видимость бэкенд (5–6 д).** **Вводим Alembic** (§5.12) — первая ревизия со всем фундаментом: `Location`,`LocationDailyCounter`,`location_drink_stops`,`app_settings`,`info_blocks`, `Order.location_id`, `StaffUser.location_id`, `OrderIn.locationId`, `locationId` в staff `/me`/типе; `PRAGMA foreign_keys=ON`. **Замещаем мок `/admin/outlets`** (удалить/мигрировать). `GET /api/locations(+id)`; `schedule_open`/`is_open`(с паузой)/`remaining`; авто-логин по телефону без OTP (§4.7); `POST /api/orders` → `payment_status="pending"`; **`mark_paid`: списание лимита под row-lock + `payment_status="paid"` + инкремент `LocationDailyCounter` (НЕ в `notify()`, §5.12)**; **`order_flow.refund_order`** (перенос инлайнового refund из `admin_orders.py`) с декрементом; лимит-гейт `409 LOCATION_LIMIT_REACHED`; 409 `LOCATION_CLOSED`/`LOCATION_PAUSED`; стоп-проверка `DRINK_UNAVAILABLE_AT_LOCATION` в `create_order`+`mark_paid`.
*DoD:* Alembic-ревизия применяется на засеянной БД (колонки реально добавлены); мок outlets удалён — одно дерево точек; неоплаченный не виден менеджеру/в статистике; **тест гонки** (K оплат не уводят за лимит L); idempotent-вебхук; refund возвращает лимит в тот же день; авто-логин (новый/существующий номер); стоп-напиток отклоняется на заказе.

**Фаза 4 — Админка: точки + сотрудники + медиа + контент + настройки + аналитика (5–6 д).** Один роутер `admin_locations.py` (пер-эндпоинтные гарды, §5.12). `/admin/locations` (CRUD: описание/адрес/фото/**часы по дням**/лимит/**пауза super_admin**/**стоп-лист**, sold/remaining, опц. история, `adjust-day`); **`staff_users.location_id` в `/admin/staff`** (+ `/me`/тип); **доступ менеджера — только своя точка (фильтр + 403) + sticky-панель остатка (§5.16) + быстрый стоп-лист (§5.15)**; **дашборд `?location_id=`** (сравнение лимитов, локация менеджера, §5.10); **`/admin/content`** (CMS, §5.11.1); **`POST /api/admin/media` + drag-and-drop компонент** (MinIO, §5.13); **`/admin/settings`** (super_admin, global/per-location/env, §5.17); купоны/добавки скрыты.
*DoD:* super_admin создаёт точку (часы/лимит/фото), **ставит паузу (менеджер — не может, 403)**, стопит напиток, прикрепляет менеджера; **менеджер видит только свою точку (чужой заказ → 403), остаток своей точки, стопит напиток в своей точке**; редактирует Story/Contact/соцсети; **drag-and-drop загрузка фото/видео**; настройки: global-дефолты редактируются, env — read-only, per-location отсылает в Точки; дашборд показывает sold/limit/remaining по точкам.

**Фаза 5 — Публичный UX локаций + мои заказы + инфо (3–4 д).** Выбор локации перед `/order` (остаток/часы/sold-out/closed/paused); индикатор остатка; обработка 409; WS/поллинг; **`/orders` с авто-логином по телефону (§4.4/§4.7)**; **`/profile` без купонов**; оценка без купона; `/info` (4 секции, контент из `info_blocks`).
*DoD:* нельзя заказать в закрытой/распроданной/паузнутой точке; гость по телефону подгружает историю; профиль без купонов; остаток в реальном времени; инфо-страница берёт Story/Contact из админки.

**Фаза 6 — Связка и регрессия (2–3 д).** Сквозной флоу GRABZI: локация → `/order` (гость по телефону) → оплата → `payment_status="paid"` → статус готовки → «I'm here» → оценка. Кросс-браузер desktop+mobile; регресс ролей админки.
*DoD:* зелёный e2e на desktop и mobile; регресс ролей админки; чек-лист §11 пройден.

*Ориентир: ~4–5 недель одним фронт+бэк; быстрее при параллели. Экономия заказчику — бэкенд+админка переиспользуются из Juicy, а не пишутся заново.*

---

## 10. Карта переиспользования из Juicy

- **Бэкенд+админка — основа из Juicy, переиспользуем целиком:** `users/staff/catalog/orders`, флоу заказа, статусы, «я на месте», оценки, дашборд, WS, Stripe, весь `/admin/*`. Купоны/добавки — в коде есть, не выводим. **Вводим Alembic** (§5.12). **Дорабатываем/добавляем:** `locations`(описание/часы/лимит-nullable/**пауза**/фото),`location_daily_counters`,**`location_drink_stops`**,**`app_settings`**,(опц.)`location_status_events`,`info_blocks`; `orders.location_id`,**`staff_users.location_id`**(+`/me`/тип),`OrderIn.locationId`; авто-логин по телефону (§4.7); списание/возврат лимита в `mark_paid`/`order_flow.refund_order` (на `payment_status`, не в `notify()`); лимит-гейт+пауза+стоп в helper-доступности; `is_open`(расписание+пауза); доступ менеджера по локации; дашборд `?location_id=`; **storage-слой+MinIO+`/api/admin/media`**; **CMS `/admin/content`**; **`/admin/settings`**; единый WS-канал `location:{id}`. **Замещаем мок `/admin/outlets`.**
- **Фронт публичный GRABZI — отдельное приложение (логика из Juicy, дизайн GRABZI):** `/home /order /product/[slug](опц.) /orders /profile /auth/*` + новые `/outlets`,`/info`. Корзина/онбординг/ввод-имени — не используются (Р3.1/Р9).

---

## 11. Тест-чеклист (приёмка)
- [ ] Лимит: нельзя продать больше `daily_drink_limit` за день (вкл. гонку параллельных оплат).
- [ ] Часы: заказ в закрытой точке отклоняется (`LOCATION_CLOSED`), `next_open_at` корректен в TZ.
- [ ] Refund в тот же день возвращает напитки в лимит; в другой день — нет.
- [ ] Граница суток считается в TZ локации.
- [ ] **Без лимита (`null`):** заказы у такой точки не ограничиваются, но `committed_drinks` всё равно растёт для аналитики.
- [ ] **Часы по дням недели:** разное время открытия/закрытия по дням применяется; выходной день блокирует заказ; часы выводятся на публичной странице локаций.
- [ ] **Привязка сотрудника к локации:** менеджер видит/берёт заказы только своей точки; чужой заказ → 403; супер-админ видит все.
- [ ] **Аналитика:** дашборд показывает sold/limit/remaining по каждой точке и локацию менеджера; менеджер на рабочем экране видит остаток своей точки.
- [ ] **Операционная пауза точки (super_admin only):** «Pause» блокирует заказ (`LOCATION_PAUSED`) отдельно от расписания/`is_active`; **менеджер паузу НЕ может (403)**; «Resume» возвращает; (опц.) история открытий/закрытий с длительностью.
- [ ] **Стоп-лист по локации:** menager своей точки/super_admin стопит напиток → в каталоге «sold out», заказ отклоняется `DRINK_UNAVAILABLE_AT_LOCATION`; глобальный `hidden` и локальный стоп независимы.
- [ ] **Лимит-гейт на сервере:** при `sold ≥ limit` заказ реально отклоняется `409 LOCATION_LIMIT_REACHED` (панель не только информативна).
- [ ] **Миграции:** Alembic-ревизия реально добавляет колонки на засеянной БД (`create_all` бы не добавил); `PRAGMA foreign_keys=ON` в SQLite.
- [ ] **Замещение outlets:** мок `/admin/outlets` удалён/мигрирован — в админке одно дерево точек.
- [ ] **Счётчик не в `notify()`:** инкремент в `mark_paid`, декремент в `order_flow.refund_order`; `notify()` остаётся чистым.
- [ ] **Контент-CMS:** Story/Contact/соцсети редактируются из `/admin/content` и сразу видны на `/info` без релиза.
- [ ] **Загрузка медиа:** drag-and-drop фото/видео в админке (MinIO); в БД — относительный ключ, URL собирается на чтении.
- [ ] **Настройки:** `/admin/settings` (super_admin) правит global-дефолты; env — read-only; per-location НЕ дублируется (отсылка в Точки).
- [ ] **Заказ фиксирует `location_id`** (видно в истории/аналитике).
- [ ] Каталог без добавок: заказ создаётся без addon-строк; цена = base×qty; **купоны не выпускаются** (дизлайк без купона).
- [ ] **Создание заказа — одна страница:** выбор категории показывает все её напитки со счётчиками; превышение остатка → модалка + блок Pay.
- [ ] **Оплата-then-видимость:** `payment_status="pending"` скрыт от менеджера и статистики, лимит не тронут; после оплаты → `paid` + списание лимита; повторный вебхук idempotent; зависшие чистятся.
- [ ] **Авто-логин по телефону без OTP:** новый телефон → создаётся юзер + токен; существующий → находится и авторизуется; заказ привязан, `user_id` проставлен.
- [ ] **Мои заказы:** ввод телефона → авто-логин → история этого пользователя (OTP сейчас не требуется; усиление — флагом на будущее).
- [ ] **Переиспользование:** бэкенд+админка взяты из Juicy и доработаны; неиспользуемое (добавки/купоны) не удалено и не ломает работу.
- [ ] Desktop и mobile: навигация, каталог-сетки, страница заказа, выбор локации.
- [ ] EN-only публичная часть; переключатель языка скрыт.
- [ ] **AR-задел (§3.3.A):** публичные строки через словарь (нет хардкода); вёрстка на логических CSS-свойствах; контент-поля — JSON i18n. *(Полный AR/RTL — отдельный модуль, не в базовом скоупе.)*
- [ ] Остаток обновляется в реальном времени (WS) и в фолбэке (поллинг).
- [ ] Ролевой доступ админки (manager vs super_admin) сохранён.
- [ ] Сквозной флоу заказа зелёный на обеих версиях.

---

## 12. Открытые вопросы (с дефолтами, не блокируют)
1. `/info` vs `/about` (дефолт `/info`); контент из админки (дефолт — да, `info_blocks`).
2. Деталка напитка (Р3.2) — дефолт **выкл** (опц. модуль).
3. Перенос `is_free_treat_day` («free treat day») из GRABZI — **вне базового скоупа** (позже).
4. Количество локаций на старте и их реальные адреса/координаты/часы — уточнить у заказчика для сидов.
5. Apple Pay — включаем при готовности Stripe-аккаунта.
6. **Арабская версия (§3.3.B)** — под вопросом; в базовый скоуп не входит, отдельный модуль по подтверждению.
7. Менеджер за **несколькими** точками? Дефолт — **одна** (иначе `staff_users.location_id` → m2m `staff_locations`).
8. Где живёт код GRABZI — отдельный репозиторий (копия Juicy-бэкенда) или ветка от Juicy. Дефолт — как удобнее разработке; это проект GRABZI на базе кода Juicy (§A).
9. **OTP при входе/истории** — сейчас **выключен** (`auth_otp_enabled=false`), авто-логин по телефону без кода (§4.7). На будущее можно включить флагом (код в Juicy есть), чтобы подтверждать владение номером.
10. **`/menu` vs `/order`** — в GRABZI это **один экран** (страница создания заказа §4.2 = просмотр напитков по категориям + счётчики). Отдельного browse-only `/menu` не делаем, чтобы не дублировать; пункт «Menu» в навигации ведёт на `/order`.
11. **Где живёт фронт GRABZI** — отдельное Next.js-приложение (новая папка/проект), использует свой API-клиент к доработанному бэкенду Juicy. Juicy-фронт (`app/`) не трогаем.
12. ✅ **РЕШЕНО — медиа: локальный S3 (MinIO) + drag-and-drop** `POST /api/admin/media` (§5.13). В БД — относительный ключ, URL на чтении.
13. ✅ **РЕШЕНО — главная hardcode в v1:** hero/баннеры из конфигурации темы GRABZI, не из админки. CMS-hero — позже при необходимости.
14. ✅ **РЕШЕНО — по-локационный стоп-лист ДЕЛАЕМ** (§5.15): `location_drink_stops`, отдельная ось от глобального `hidden`. Управляет менеджер своей точки + super_admin.
15. ✅ **РЕШЕНО — экран настроек ДЕЛАЕМ** `/admin/settings` (super_admin, §5.17): global-дефолты в `app_settings`, env read-only, per-location в Точках.
16. ✅ **РЕШЕНО — пауза точки только super_admin** (§5.14, пересмотр Р13). Менеджер — read-only статус.
17. **TRN/налоговые реквизиты точки** (для чеков) — в Juicy-моке у аутлета был TRN; если нужны в чеках, добавить `locations.trn` (сейчас не закладываем).
18. **Авто-возобновление паузы по таймеру** — вне скоупа (пауза снимается вручную); при нужде — `locations.paused_until` + фон.
19. **Зачёт лимита** — по `mark_paid` (оплата=продажа). Если бизнес считает по факту выдачи — переключить на `transition→completed`. Дефолт — оплата.
20. ✅ **РЕШЕНО — стек:** PostgreSQL + Poetry + FastAPI + Next.js + Docker (Р0.1); миграция SQLite→Postgres, requirements→Poetry, create_all→Alembic (§13).
21. ✅ **РЕШЕНО — VAT:** цены **VAT-inclusive** (ОАЭ-розница), отдельной VAT-строки нет, `total = sum(price×qty)` (купонов нет). Деньги — `Numeric(10,2)`/`Decimal`.
22. **Лицензия шрифта Grenadine MVB** (fonnts.com) — проверить; до развязки бренд держится на fallback-стеке (self-host под флагом). Не блокирует старт.

---

## 13. Фронт-спека, бэкенд-архитектура, тестирование (ссылки + сквозные решения)
Детальные исполняемые спеки вынесены в отдельные файлы (на них ссылается план):
- **[docs/GRABZI_FRONTEND_SPEC.md](GRABZI_FRONTEND_SPEC.md)** — фронт-архитектура Next.js 2026 (RSC/границы/i18n/zod), **дизайн-токены** (реальная мульти-схема палитры grabzi.ae), **инвентарь UI-компонентов** (что взять как есть / адаптировать / дорисовать + все состояния), **матрица состояний** всех публичных страниц (EN-copy, анти-dead-end), **удобный экран готовки заказа** `/admin/kitchen` (канбан, звук/баннер нового заказа, таймер, «на месте», остаток точки, ASCII-макеты).
- **[docs/GRABZI_BACKEND_AND_TESTING.md](GRABZI_BACKEND_AND_TESTING.md)** — чистая архитектура (Poetry/Postgres/FastAPI/Docker/Alembic, слои, sync, idempotency), **атомарный лимит в напитках**, refund-корректность, **стратегия тестирования** (pytest+testcontainers, тест гонки, vitest/RTL+MSW, Playwright, CI, чек-лист ручной приёмки).

**Сквозные решения, зафиксированные ревизией (применяются во всех модулях):**
- **Канонический реестр кодов ошибок** — один enum на бэке ↔ zod/copy-map на фронте (фронт-спека §4): `LOCATION_CLOSED/LOCATION_PAUSED/LOCATION_LIMIT_REACHED/DRINK_UNAVAILABLE_AT_LOCATION/STOCK_LESS_THAN_ORDER/...`. Дизайны изобретали разные имена — унифицировано.
- **WS-аутентификация (БЛОКЕР):** `/ws/admin/orders` и `/ws/orders/{id}` сейчас **без auth** — `ws.py` ДОЛЖЕН измениться: гейт по staff/owner-токену (query-param/subprotocol, т.к. браузер не шлёт `Authorization` на WS); `manager` — только per-location канал `admin:orders:{location_id}`. Без этого канбан и скоуп менеджера неисполнимы.
- **Лимит считается в НАПИТКАХ** (`sum quantity`, materialized `location_daily_counters` под `FOR UPDATE`), НЕ в заказах (дизайны ошибочно считали заказы). Заказ из 3 напитков = 3 единицы лимита.
- **Refund-корректность:** через `order_service.refund_order` (не inline), декремент только same-business-day, `GREATEST(0)`, idempotent, при `null`-лимите — пропуск.
- **VAT-inclusive**, деньги `Numeric(10,2)`/`Decimal` (§12.21).
- **Миграции Alembic:** 0001 baseline (= `create_all`, Float остаётся) → stamp на прод; 0002 `Float→Numeric`; 0003 GRABZI-фундамент. `PRAGMA foreign_keys=ON` для SQLite-dev.

**Тест-фаза (добавить в §9):** **Фаза 7 — Тестирование и приёмка** (параллельно/после фаз): покрытие доменных инвариантов (лимит-гонка, часы/пауза/стоп, refund, scope, авто-логин, WS-auth), состояния страниц (vitest+MSW), e2e (Playwright desktop+mobile), CI, ручной чек-лист (бэкенд-спека §J).

---

_Изменения v1→v2: добавлены desktop-версия и адаптив (§3.1); направление UI-кита изменено на GRABZI с портированием Juicy-экранов (§2, §3.2); убраны добавки из публичного флоу и админ-меню (§4); устранены противоречия v1 (EN-only в токенах, списание лимита перенесено в момент оплаты §5.3); добавлены возвраты-в-лимит (§5.4), контракты API (§5.6), миграции/сиды (§8), критерии приёмки по фазам (§9) и тест-чеклист (§11)._

_Изменения v2→v3: **pay-first** — заказ создаётся только после оплаты, введён `checkout_drafts` и `finalize_paid_order` в вебхуке (Р5, §5.1/§5.3/§5.6); уточнено: публичный сайт EN-only, в админке RU/AR временно (Р8); подтверждена сквозная нумерация (0–12)._

_Изменения v3→v4: **арабский вынесен в отдельный отложенный модуль** (Р9, §3.3); **дневной лимит сделан необязательным** (`null` = без лимита, §5.2); **часы открытия/закрытия по каждому дню недели** + публичные справочные API (§5.5); **сотрудник привязывается к локации**, доступ менеджера ограничен своей точкой (§5.9); **аналитика по локациям** (§5.10); **заказ фиксирует `location_id`**._

_Изменения v4→v5 (ключевая смена рамки): **§A — берём готовый бэкенд+админку Juicy и дорабатываем под GRABZI** (Р0); GRABZI-фронт отдельный; неиспользуемое (добавки/купоны) не выводим, код не ломаем. Публичный флоу упрощён: **убраны онбординг/ввод имени** (Р9), **убрана корзина** — заказ на **одной странице** (Р3.1), **деталка — опциональный модуль** (Р3.2), **купоны отключены** (Р8). Каталог — логикой Juicy._

_Изменения v6→v7 (ревизия админки по аудиту Juicy — закрытие пробелов управления): добавлен раздел **§5.11**: (1) **CMS контента** `/admin/content` (Story/Contact/соцсети/блоки) — теперь в скоупе, не опционально; (2) **загрузка медиа** `POST /api/admin/media` в объектное хранилище (в Juicy был только URL-ввод); (3) **операционная пауза точки** `accepting_orders` + кнопка «Pause/Resume» и `LOCATION_PAUSED` (отдельно от расписания и `is_active`); (4) **менеджер видит остаток своей точки** на рабочем экране; (5) **дашборд Juicy дорабатывается** под `?location_id=` (раньше группировки не было); (6) явная граница **«админка (данные) vs env-конфиг (инфраструктура)»**, осознанные исключения (по-локационный стоп-лист, TRN, экран настроек) вынесены в §12. Модель `locations` += `accepting_orders`,`image_url`._

_Изменения v5→v6 (ревизия на пробелы по коду Juicy): (1) **видимость заказа после оплаты — на существующем `payment_status`** Juicy (менеджерский список уже фильтрует `paid`), **новый статус `pending_payment` НЕ вводим** (§5.1/§5.3); (2) **авторизация по телефону без OTP** (§4.7): авто-логин (новый номер → создаём юзера; существующий → находим и авторизуем); (3) «Мои заказы» гостя = тот же авто-логин; (4) `/menu` = `/order` один экран, фронт GRABZI — отдельное Next-приложение._

_Изменения v8→v9 (стек + верификация сайта + фронт/бэк/тест-спеки, воркфлоу research→design→critique 11 агентов): зафиксирован **стек** Postgres/Poetry/FastAPI/Next.js/Docker (Р0.1); **перепроверен парсинг живого grabzi.ae** — исправлено ошибочное допущение «видео-петли» (реально **статичные PNG**, Р3.0/§1.3), палитра расширена до реальной мульти-схемы (§3.2); добавлены **[GRABZI_FRONTEND_SPEC.md]** (архитектура Next.js 2026, дизайн-токены, инвентарь UI-компонентов с состояниями, матрица состояний всех публичных страниц с EN-copy, удобный экран готовки `/admin/kitchen` с ASCII) и **[GRABZI_BACKEND_AND_TESTING.md]** (Poetry/Postgres/Alembic/чистые слои + стратегия тестов). Сквозные фиксы ревизии (§13): **WS-аутентификация — блокер** (ws.py должен измениться), **канонический реестр кодов ошибок** (дизайны расходились), **лимит в напитках** (дизайны ошибочно считали заказы), refund-корректность (same-day/GREATEST(0)/через order_service), **VAT-inclusive** + Numeric/Decimal, Alembic 0001 baseline/0002 Numeric/0003 фундамент, next-intl static-render, ws:// scheme, рейтинг по arrived. Источник: воркфлоу `grabzi-frontend-arch-spec`, аудит `app/`+`backend/`, curl живого сайта._

_Изменения v7→v8 (детальный дизайн админки + адверсариальная ревизия, через воркфлоу research→design→critique, заземление в коде): добавлены **§5.12–§5.17**. По вводным заказчика: **локальный S3 (MinIO) + drag-and-drop везде** (§5.13), **пауза точки — только super_admin** + опц. история `location_status_events` (§5.14, пересмотр Р13), **стоп-лист по локации ДЕЛАЕМ** `location_drink_stops` (§5.15, пересмотр Р7/§12.14), **панель остатка у менеджера** с ASCII-UX (§5.16), **экран настроек `/admin/settings`** global/per-location/env (§5.17, пересмотр §12.15). Критики выявили и исправлены сквозные проблемы (§5.12): **в проекте НЕТ Alembic** (`create_all` не делает ALTER — вводим Alembic), **существующий мок `/admin/outlets` замещаем** (а не плодим второй раздел), **счётчик лимита — НЕ в `notify()`** (явный шаг в `mark_paid` + `order_flow.refund_order`), **один роутер `admin_locations.py` с пер-эндпоинтными гардами**, **`OrderIn.locationId` + `locationId` в staff `/me`/типе** (иначе скоуп менеджера не работает), **медиа хранит относительный ключ** (URL на чтении — честный provider-swap), **SQLite `PRAGMA foreign_keys=ON`**, статус точки — вычисляемый, не хранимая enum, единый WS-канал `location:{id}`. Источник: воркфлоу `grabzi-admin-design` (12 агентов), аудит `backend/app/*`+`app/app/admin/*`._
