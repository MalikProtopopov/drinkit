"""Регистрация всех ORM-моделей в Base.metadata.

Импортируем каждый модуль здесь, чтобы любой доступ к app.models.* (и create_all) видел все
таблицы независимо от пути импорта. Раньше регистрация держалась на транзитивных импортах
роутеров — пропущенный импорт молча ронял создание таблиц.
"""
from . import catalog, orders, outlet, users  # noqa: F401
