# Линтер бэкенда. Запуск: .venv/bin/ruff check app
# Фокус на высокосигнальном: pyflakes (F) — неиспользуемые импорты/переменные,
# необъявленные имена; E9 — синтаксические ошибки. Стиль/длину строк не трогаем.
line-length = 120

[lint]
select = ["F", "E9"]
